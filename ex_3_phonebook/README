Здравствуйте! Это мой проект 'Телефонная книга', выполненный на основе библиотек telebot, openpyxl, pandas, os. 
Хранение данных, добавление записи в файл, экспорт и импорт осуществляется с помощью таблиц exсel, 
команды вводятся пользователем через Telegram bot.
Чтобы начать работу Вам необходимо создать бота с помощью @BotFather и копировать токен в стркоу кода №9 (TOKEN = '53434.....')
При добавлении пользователем новой записи через консоль формируется ID по номеру строки.
В сравнении с предыдущей версией исправлено добавление ID при импорте файла (ID добавляется автоматически).
Для установки необходимых библиотек необходимо ввести следующие команды в терминале:
1.pip install pytelegrambotapi
При выполнении этой команды установится библиотека версии pyTelegramBotAPI==4.8.0
2.pip install pandas 
При выполнении этой команды установится библиотека версии pandas==1.5.2
3.pip install openpyxl
При выполнении этой команды установится библиотека версии  openpyxl==3.0.10 
4.Модуль os является компонетом стандартной библитеки. 
Для проверки наличия необходимых библиотек в командной строке необходимо ввести pip freeze, вы увидите все установлнные у вас библиотеки.